{% extends 'base.html.twig' %}

{% block body %}
    <h1>Socioprofessionalgroup</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ socioprofessionalGroup.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ socioprofessionalGroup.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('socioprofessionalgroup_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('socioprofessionalgroup_edit', { 'id': socioprofessionalGroup.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
